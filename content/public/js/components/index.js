import "./theme-selector.js";
import "./rotating-tagline.js";
// Eager, not lazy-loaded: the page background is needed immediately and
// unconditionally, and it has no visible box for an IntersectionObserver to
// watch.
import "./maze-background.js";
