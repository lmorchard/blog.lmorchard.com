{
  "compilerOptions": {
    "checkJs": false,     // disable TypeScript error checking in JS files
    "allowJs": true       // (optional) makes sure JS files are still considered
  },
  "include": ["./lib/**/*", "./templates/**/*"]
}
